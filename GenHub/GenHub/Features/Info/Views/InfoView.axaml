<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GenHub.Features.Info.ViewModels"
             xmlns:material="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
             xmlns:infoViews="clr-namespace:GenHub.Features.Info.Views"
             xmlns:controls="clr-namespace:GenHub.Common.Controls"
             xmlns:localization="clr-namespace:GenHub.Common.Markup;assembly=GenHub"
             mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="700"
             x:Class="GenHub.Features.Info.Views.InfoView"
             x:DataType="vm:InfoViewModel">

  <UserControl.Resources>
  </UserControl.Resources>

  <UserControl.DataTemplates>
    <DataTemplate DataType="vm:FaqSectionViewModel">
      <infoViews:FaqSectionView />
    </DataTemplate>
    <DataTemplate DataType="vm:GenHubInfoSectionViewModel">
      <infoViews:GenHubInfoSectionView />
    </DataTemplate>
  </UserControl.DataTemplates>

  <UserControl.Styles>
    <!-- Override local styles to align with theme -->
    <Style Selector="ListBoxItem">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Margin" Value="0" />
    </Style>

    <Style Selector="TextBlock.header">
      <Setter Property="FontSize" Value="28"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
      <Setter Property="Margin" Value="0,0,0,20"/>
    </Style>
  </UserControl.Styles>

  <controls:SidebarLayout PaneTitle="{localization:Localize Info.Sidebar.Title}"
                          ItemsSource="{Binding SidebarItems}"
                          SelectedItem="{Binding SelectedSidebarItem, Mode=TwoWay}"
                          IsPaneOpen="{Binding IsPaneOpen, Mode=TwoWay}"
                          OpenPaneLength="{Binding OpenPaneLength, Mode=TwoWay}">

    <controls:SidebarLayout.PaneHeader>
        <ComboBox ItemsSource="{Binding Modules}"
                  SelectedItem="{Binding SelectedModule}"
                  HorizontalAlignment="Stretch">
              <ComboBox.ItemTemplate>
                  <DataTemplate>
                      <TextBlock Text="{Binding Converter={StaticResource LocalizedInfoModuleNameConverter}}" Foreground="{DynamicResource TextPrimary}" />
                  </DataTemplate>
              </ComboBox.ItemTemplate>
        </ComboBox>
    </controls:SidebarLayout.PaneHeader>

    <controls:SidebarLayout.ItemTemplate>
        <DataTemplate>
            <ContentControl Content="{Binding}">
                <ContentControl.DataTemplates>
                    <!-- Guide Section Item -->
                    <DataTemplate DataType="vm:InfoSectionViewModel">
                        <Grid ColumnDefinitions="*" Margin="4,2">
                            <TextBlock Text="{Binding Title}" VerticalAlignment="Center" FontSize="14" FontWeight="Medium" TextWrapping="Wrap" Foreground="{DynamicResource TextPrimary}"/>
                        </Grid>
                    </DataTemplate>
                    <!-- Zero Hour FAQ Category -->
                    <DataTemplate DataType="vm:FaqCategoryViewModel">
                        <Grid ColumnDefinitions="Auto,*" Margin="4,2">
                             <material:MaterialIcon Kind="HelpCircleOutline"
                                                    Width="20"
                                                    Height="20"
                                                    Foreground="{DynamicResource AccentBrush}"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,12,0"/>
                             <TextBlock Grid.Column="1"
                                        Text="{Binding Title}"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        FontWeight="Medium"
                                        TextWrapping="Wrap"
                                        Foreground="{DynamicResource TextPrimary}"/>
                        </Grid>
                    </DataTemplate>
                </ContentControl.DataTemplates>
            </ContentControl>
        </DataTemplate>
    </controls:SidebarLayout.ItemTemplate>

    <!-- Content Area -->
    <Panel>
        <!-- Main Content (Guide or Zero Hour) -->
        <ContentControl Content="{Binding SelectedSection}" />
    </Panel>
  </controls:SidebarLayout>

</UserControl>
