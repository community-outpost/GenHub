<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GenHub.Features.Tools.ViewModels"
             xmlns:interfaces="clr-namespace:GenHub.Core.Interfaces.Tools;assembly=GenHub.Core"
             xmlns:converters="clr-namespace:GenHub.Infrastructure.Converters"
             xmlns:controls="clr-namespace:GenHub.Common.Controls"
             xmlns:material="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
             xmlns:localization="clr-namespace:GenHub.Common.Markup"
             mc:Ignorable="d" d:DesignWidth="1100" d:DesignHeight="700"
             x:Class="GenHub.Features.Tools.Views.ToolsView"
             x:DataType="vm:ToolsViewModel"
             x:Name="Root">

  <UserControl.Resources>
    <converters:ObjectToBoolConverter x:Key="ObjectToBoolConverter" IsNullValue="False" IsNotNullValue="True" />
    <converters:LocalizedToolNameConverter x:Key="LocalizedToolNameConverter" />
    <converters:LocalizedToolDescriptionConverter x:Key="LocalizedToolDescriptionConverter" />
  </UserControl.Resources>

  <UserControl.Styles>
    <Style Selector="Border.tool-list-card">
      <Setter Property="Background" Value="{DynamicResource CardBackground}" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
      <Setter Property="BorderThickness" Value="0,0,1,0" />
    </Style>

    <Style Selector="Border.tool-content-card">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Margin" Value="10" />
    </Style>

    <Style Selector="Border.tool-content-card.fullscreen">
      <Setter Property="Margin" Value="0" />
      <Setter Property="CornerRadius" Value="0" />
    </Style>

    <Style Selector="Button.tools-action-btn">
      <Setter Property="Background" Value="{DynamicResource SurfaceElevatedBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="Padding" Value="12,8" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Transitions">
        <Transitions>
          <BrushTransition Property="Background" Duration="0:0:0.15" />
          <BrushTransition Property="BorderBrush" Duration="0:0:0.15" />
          <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.15" />
        </Transitions>
      </Setter>
    </Style>

    <Style Selector="Button.tools-action-btn:pointerover">
      <Setter Property="Background" Value="{DynamicResource AccentTintBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
    </Style>

    <Style Selector="Button.tools-action-btn:pressed">
      <Setter Property="RenderTransform" Value="scale(0.98)" />
    </Style>

    <Style Selector="Button.dialog-close-btn">
      <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Padding" Value="16,8" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Transitions">
        <Transitions>
          <BrushTransition Property="Background" Duration="0:0:0.15" />
        </Transitions>
      </Setter>
    </Style>

    <Style Selector="Button.dialog-close-btn:pointerover">
      <Setter Property="Background" Value="{DynamicResource SurfaceElevatedBrush}" />
    </Style>

    <Style Selector="Border.status-banner">
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Padding" Value="12,6" />
      <Setter Property="Margin" Value="10,0,0,0" />
      <Setter Property="HorizontalAlignment" Value="Right" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style Selector="Border.status-banner.success">
      <Setter Property="Background" Value="{DynamicResource SuccessTintBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}" />
      <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style Selector="Border.status-banner.error">
      <Setter Property="Background" Value="{DynamicResource ErrorTintBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}" />
      <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style Selector="Border.status-banner.info">
      <Setter Property="Background" Value="{DynamicResource InfoTintBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource InfoBrush}" />
      <Setter Property="BorderThickness" Value="1" />
    </Style>
  </UserControl.Styles>

  <Grid>
    <!-- Main Sidebar Layout -->
    <controls:SidebarLayout PaneTitle="{localization:Localize Tools.Label.InstalledTools}"
                            ItemsSource="{Binding InstalledTools}"
                            SelectedItem="{Binding SelectedTool, Mode=TwoWay}"
                            IsPaneOpen="{Binding IsPaneOpen, Mode=TwoWay}"
                            OpenPaneLength="{Binding OpenPaneLength, Mode=TwoWay}"
                            ItemTemplate="{StaticResource ToolItemTemplate}">

      <controls:SidebarLayout.Resources>
        <DataTemplate x:Key="ToolItemTemplate" DataType="interfaces:IToolPlugin">
            <Grid ColumnDefinitions="Auto,*,Auto" VerticalAlignment="Center" Margin="0,4">
             <Panel Grid.Column="0" Width="24" Height="24" Margin="4,0,12,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                 <material:MaterialIcon Kind="Tools"
                                      Width="20"
                                      Height="20"
                                      Foreground="{DynamicResource AccentBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      IsVisible="{Binding Metadata.IconPath, Converter={x:Static StringConverters.IsNullOrEmpty}}"/>
                 <Image Source="{Binding Metadata.IconPath, Converter={StaticResource StringToImageConverter}}"
                        Width="24"
                        Height="24"
                        Stretch="Uniform"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        IsVisible="{Binding Metadata.IconPath, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
             </Panel>

             <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
              <TextBlock Text="{Binding, Converter={StaticResource LocalizedToolNameConverter}}" FontWeight="SemiBold" FontSize="13.5" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}"/>
              <TextBlock Text="{Binding Metadata.Version, StringFormat='v{0}'}" 
                         FontSize="11.5" Foreground="{DynamicResource TextSecondary}" 
                         VerticalAlignment="Center"
                         IsVisible="{Binding Metadata.Version, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
             </StackPanel>
          </Grid>
        </DataTemplate>
      </controls:SidebarLayout.Resources>

      <controls:SidebarLayout.PaneHeader>
          <Button Command="{Binding AddToolCommand}"
                  Classes="tools-action-btn"
                  HorizontalAlignment="Stretch">
              <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                  <material:MaterialIcon Kind="Plus" Width="18" Height="18" Foreground="{DynamicResource AccentBrush}"/>
                  <TextBlock Text="{localization:Localize Tools.Button.AddToolAction}" VerticalAlignment="Center"/>
              </StackPanel>
          </Button>
      </controls:SidebarLayout.PaneHeader>

      <controls:SidebarLayout.PaneFooter>
          <Button Command="{Binding RefreshToolsCommand}"
                  Classes="tools-action-btn"
                  HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
               <material:MaterialIcon Kind="Refresh" Width="16" Height="16" Foreground="{DynamicResource AccentBrush}"/>
               <TextBlock Text="{localization:Localize Tools.Button.RefreshToolsAction}" VerticalAlignment="Center"/>
            </StackPanel>
          </Button>
      </controls:SidebarLayout.PaneFooter>

      <Grid RowDefinitions="Auto,*">
         <!-- Header / Status Area -->
         <Grid Grid.Row="0" Margin="20" ColumnDefinitions="Auto,*" IsVisible="{Binding !SelectedTool.Metadata.IsFullScreen}">
           <TextBlock Text="{Binding SelectedTool, Converter={StaticResource LocalizedToolNameConverter}, FallbackValue='GenHub Tools'}" Classes="header" />

           <!-- Status Messages -->
           <Border Grid.Column="1"
                   Classes="status-banner"
                   Classes.success="{Binding IsStatusSuccess}"
                   Classes.error="{Binding IsStatusError}"
                   Classes.info="{Binding IsStatusInfo}"
                   IsVisible="{Binding IsStatusVisible}">
              <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" />
           </Border>
         </Grid>

         <!-- Content Area -->
         <Panel Grid.Row="1" Margin="10">
            <!-- Empty State -->
            <Grid IsVisible="{Binding SelectedTool, Converter={x:Static ObjectConverters.IsNull}}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                <Border Background="{DynamicResource SurfaceElevatedBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="16"
                        Padding="40,32"
                        MaxWidth="520"
                        HorizontalAlignment="Center">
                    <StackPanel Spacing="16" HorizontalAlignment="Center">
                        <Border Width="64"
                                Height="64"
                                CornerRadius="32"
                                Background="{DynamicResource SurfaceCardBrush}"
                                BorderBrush="{DynamicResource BorderSubtleBrush}"
                                BorderThickness="1"
                                HorizontalAlignment="Center">
                            <material:MaterialIcon Kind="WrenchOutline"
                                                   Width="32"
                                                   Height="32"
                                                   Foreground="{DynamicResource AccentBrush}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="{localization:Localize Tools.EmptyState.SelectToolTitle}"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{localization:Localize Tools.EmptyState.SelectToolDescription}"
                                   FontSize="14"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Foreground="{DynamicResource TextSecondary}"
                                   HorizontalAlignment="Center"
                                   LineHeight="20"/>
                        <Button Command="{Binding OpenToolsInfoCommand}"
                                Classes="tools-action-btn"
                                HorizontalAlignment="Center"
                                Margin="0,8,0,0">
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <material:MaterialIcon Kind="BookOpenOutline" Width="18" Height="18" Foreground="{DynamicResource AccentBrush}"/>
                                <TextBlock Text="{localization:Localize Tools.Button.ReadInfoTab}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Tool Content -->
            <Border Classes="tool-content-card"
                    Classes.fullscreen="{Binding SelectedTool.Metadata.IsFullScreen}"
                    IsVisible="{Binding SelectedTool, Converter={x:Static ObjectConverters.IsNotNull}}">
               <ContentControl Content="{Binding CurrentToolControl}" />
            </Border>
         </Panel>
       </Grid>
    </controls:SidebarLayout>

    <!-- Details Dialog -->
    <Panel IsVisible="{Binding IsDetailsDialogOpen}" Background="#CC000000" ZIndex="100">
        <Border Background="{DynamicResource SurfaceElevatedBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MinWidth="420"
                BoxShadow="0 16 36 0 #80000000">
            <StackPanel Spacing="16">
                <TextBlock Text="{localization:Localize Tools.Dialog.ToolDetails}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                <StackPanel Spacing="12" DataContext="{Binding ToolForDetails}" x:DataType="interfaces:IToolPlugin">
                    <Grid ColumnDefinitions="Auto,*" Margin="0,0,0,4" VerticalAlignment="Center">
                        <Panel Grid.Column="0" Width="40" Height="40" Margin="0,0,12,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <material:MaterialIcon Kind="Tools"
                                                  Width="32"
                                                  Height="32"
                                                  Foreground="{DynamicResource AccentBrush}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  IsVisible="{Binding Metadata.IconPath, Converter={x:Static StringConverters.IsNullOrEmpty}}"/>
                            <Image Source="{Binding Metadata.IconPath, Converter={StaticResource StringToImageConverter}}"
                                   Width="40"
                                   Height="40"
                                   Stretch="Uniform"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   IsVisible="{Binding Metadata.IconPath, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                        </Panel>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                            <TextBlock Text="{Binding, Converter={StaticResource LocalizedToolNameConverter}}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="{Binding Metadata.Version, StringFormat='v{0}'}"
                                       FontSize="12" Foreground="{DynamicResource TextSecondary}"
                                       IsVisible="{Binding Metadata.Version, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                        </StackPanel>
                    </Grid>
                    <StackPanel Spacing="2">
                        <TextBlock Text="{localization:Localize Tools.Label.Author}" FontWeight="SemiBold" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock Text="{Binding Metadata.Author}" FontSize="14" Foreground="{DynamicResource TextPrimary}"/>
                    </StackPanel>
                    <StackPanel Spacing="2" IsVisible="{Binding Metadata.Description, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                        <TextBlock Text="{localization:Localize Tools.Label.Description}" FontWeight="SemiBold" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBlock Text="{Binding, Converter={StaticResource LocalizedToolDescriptionConverter}}" TextWrapping="Wrap" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>
                <Button Command="{Binding CloseDetailsDialogCommand}"
                        Content="{localization:Localize Common.Button.Close}"
                        Classes="dialog-close-btn"
                        HorizontalAlignment="Right"
                        Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Panel>

  </Grid>
</UserControl>
